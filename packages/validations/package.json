{
  "name": "validations",
  "version": "0.0.0",
  "main": "./dist/index.js",
  "source": "./src/index.ts",
  "types": "./dist/index.d.ts",
  "files": [
    "dist/**"
  ],
  "scripts": {
    "build": "etsc",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist && rm -rf dist",
    "dev": "nodemon",
    "lint": "eslint \"src/**/*.ts*\""
  },
  "dependencies": {
    "zod": "3.20.2"
  },
  "devDependencies": {
    "typescript": "4.7.4"
  }
}
